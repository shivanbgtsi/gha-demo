name: Test github action
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Echo name
        run: |
          echo "Welcome to github action demo temp" 
